# Диагностика: работают ли Actions в этом аккаунте вообще.
name: Ping

on:
  workflow_dispatch:

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - run: echo "linux ok"
  mac:
    runs-on: macos-latest
    steps:
      - run: sw_vers && xcodebuild -version
